<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mayankjain.creativenotes">

    <application
        android:allowBackup="true"
        android:icon="@drawable/brush"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".display_update.displayActivity" />
        <activity android:name=".display_update.to_display_fav_urg" />

        <provider
            android:name=".database.noteProvider"
            android:authorities="com.example.mayankjain.creativenotes"
            android:exported="false" />

        <activity android:name=".display_update.to_get_diisplay_data" />
        <activity
            android:name=".update_color_dataBase.updateColorsActivity"
            android:label="@string/title_activity_update_colors"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".update_color_dataBase.enter_to_update_color" />
        <activity android:name=".about.aboutActivity" />
        <activity android:name=".set_up_theme.change_theme"></activity>
    </application>

</manifest>